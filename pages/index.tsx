import Layout from '../components/Layout'; import services from '../data/services.json';
export default function Home(){ const sections=(services as any).sections as any[]; return (<Layout title="FixFind — Trusted Fixers. Real Solutions."><section className="container" style={{paddingTop:16}}><h1>FixFind</h1><p className="muted">Premium marketplace for local fixers — with face verification.</p><div style={{display:'grid',gridTemplateColumns:'repeat(6,minmax(0,1fr))',gap:12}}>{sections.map((s:any)=><a key={s.key} href="/verify-face" className="card" style={{textAlign:'center'}}>{s.icon} {s.label}</a>)}</div></section></Layout>); }